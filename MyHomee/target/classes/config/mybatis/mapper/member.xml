<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
	
	<select id="checkLogin" resultType="int" parameterType="member">
		select no from member where id=#{id} and password=#{password}
	</select>
	
	<select id="findId" resultType="String" parameterType="member">
		select id from member where name = #{name} and tel1 = #{tel1} and tel2 = #{tel2} and tel3 = #{tel3}
	</select>
	
	<select id="findPw" resultType="String" parameterType="member">
		select password from member where id=#{id} and name = #{name} and tel1 = #{tel1} and tel2 = #{tel2} and tel3 = #{tel3}
	</select>
	
	<insert id="insert" parameterType="member">
		insert into member values(member_seq.nextval,#{id},#{password},#{name},#{email},#{tel1},#{tel2},#{tel3})
	</insert>
	
	<select id="selectOne" parameterType="int" resultType="member">
		select * from member where no=#{no}
	</select>
	
	<update id="update" parameterType="member">
		update member set email=#{email},tel1=#{tel1},tel2=#{tel2},tel3=#{tel3} where no=#{no}
	</update>
	
	<delete id="delete" parameterType="int">
		delete member where no=#{no}
	</delete>
	
	<select id="checkId" parameterType="String" resultType="String">
		select id from member where id=#{id}
	</select>
	
</mapper>



























































